.topfix
.container
    .col--sm-10.col--sm-offset-1
        h1 ログアウト
        p
            span(ng-if="Auth.active()")
                a(ui-sref="profile") {{Auth.user().handle_name}}
                | としてすでにログインしています。
            span(ng-if="!Auth.active()") ログインしていません。
        p
            button.btn(ng-click="performLogout()" ng-disabled="!Auth.active()") Logout
