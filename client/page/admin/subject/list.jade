p
    a.btn.btn--sm.btn--primary(ui-sref=".new") 新規作成

table.table.table--bordered.table--condensed
    tr
        th.text-small
            nobr 日本語名 or 英語名
        td
            input.search.search--condensed(
                placeholder="正規表現が使えます"
                ng-model="titleFilter.value"
                ng-model-options="{ debounce: {'default': 500, 'blur': 0} }")

table.table.table--condensed
    thead.table__head
        tr
            th
                a(ng-click="subjects.setSorterOrInverse(idSorter)") ID
            th
                a(ng-click="subjects.setSorterOrInverse(titleJaSorter)") 名前
            th
                a(ng-click="subjects.setSorterOrInverse(titleEnSorter)") 名前(英語)
            th 開講期
    tbody.table__body
        tr(ng-repeat="subject in subjects.transformed()")
            td {{subject.id}}
            td
                a(ui-sref="admin.subject.detail({id:subject.id})")
                    | {{subject.title_ja}}
            td
                | {{subject.title_en}}
            td
                a(ui-sref="admin.semester({subject:subject.title_en})") {{subject.title_ja}}の開講期
