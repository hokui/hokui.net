.col-sm-8.col-sm-push-2.col-md-6.col-md-push-3.col-lg-4.col-lg-push-4
    h1 Login

    form(v-on="submit: performLogin($event)" novalidate)
        .control-group
            input.control(
                v-el="email"
                type="email"
                name="email"
                v-model="credentials.email"
                placeholder="Email address"
                v-validate="required")
        .control-group
            input.control(
                v-el="password"
                type="password"
                name="password"
                v-model="credentials.password"
                placeholder="Password"
                v-validate="required")
        .control-group
            input.control(type="checkbox" name="keepLogin" v-model="keepLogin" id="keepLogin")
            label.control-label(
                for="keepLogin"
                title="公共および他人のPCを使用の場合はチェックをいれないでください"
                ) ログイン状態を保存する

        .control-group
            button.btn.btn-green.btn-block(type="submit") ログイン

    hr
    p
        a.btn.btn-ghost.btn-green.btn-block(href="/signup") ユーザー登録申請
    p
        | ログイン・登録できないという方は
        a(href="/help") こちら
        | を御覧ください
