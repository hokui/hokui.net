.notify(ng-controller='NotifyCtrl', ng-class="{ 'notify--shown': Notification.isVisible() }")
    .alert-container
        .alert(ng-class="Notification.alertClass()")
            | {{Notification.getMessage()}}
            button.alert__close(ng-click="Notification.hide()")
                i.fa.fa-times
